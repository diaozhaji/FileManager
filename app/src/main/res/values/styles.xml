<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="ToolbarTitleText" parent="TextAppearance.AppCompat.Widget.ActionBar.Title">
        <item name="android:textSize">18sp</item>
    </style>

    <style name="CompactToolbar" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="actionBarSize">48dp</item>
    </style>

    <style name="Toolbar.Title" parent="TextAppearance.AppCompat.Title">
        <item name="android:textSize">16sp</item>
        <item name="android:textColor">@color/white</item>
        <item name="fontFamily">sans-serif-medium</item>
    </style>

    <!-- res/values/styles.xml -->
    <style name="CustomSeekBar" parent="Widget.AppCompat.SeekBar">
        <item name="android:progressBackgroundTint">#33000000</item>  <!-- 轨道背景色 -->
        <item name="android:progressTint">?attr/colorPrimary</item>   <!-- 进度条颜色 -->
        <item name="android:thumbTint">?attr/colorPrimary</item>       <!-- 滑块颜色 -->
        <item name="android:minHeight">8dp</item>                     <!-- 最小高度 -->
    </style>
</resources>